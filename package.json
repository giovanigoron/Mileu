{
  "name": "mileu",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Mileu — a digital recipe notebook styled like an early-2000s Balkan home cookbook.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "gh-pages": "^6.2.0",
    "vite": "^6.0.5"
  }
}
